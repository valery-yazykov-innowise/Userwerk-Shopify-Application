version: '3.9'
services:
  php-apache:
    build:
      context: ./php-apache
      dockerfile: Dockerfile
    image: php-8.1:apache
    volumes:
      - ./www/:/var/www/html/
    ports:
      - "8080:80"
    container_name: php-apache
